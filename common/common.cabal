name: common
version: 0.1
cabal-version: >= 1.2
build-type: Simple

library
  hs-source-dirs: src
  build-depends: base
               , obelisk-executable-config
               , obelisk-route
               , text

  exposed-modules:
    Common.Api
    Common.Route
